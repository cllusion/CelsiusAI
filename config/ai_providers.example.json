{
  "providers": [
    {
      "name": "gemini",
      "type": "generic",
      "endpoint": "https://YOUR_PROVIDER_ENDPOINT_HERE",
      "headers": {
        "Authorization": "Bearer YOUR_API_KEY_HERE",
        "Content-Type": "application/json"
      },
      "fetch_payload": {
        "prompt": "Generate a short safe conversation between two AIs about system monitoring (max length 200).",
        "max_tokens": 200
      }
    }
  ]
}
