"""
This script was moved to archive/prototypes because it depends on optional
third-party libraries (google.generativeai, google-auth) and prototype
connectors. See `archive/prototypes/fetch_gemini_test.py` for the original.

To keep the workspace clean for development and static analysis, this
placeholder exits early.
"""

import sys

print("fetch_gemini_test is archived. See archive/prototypes/fetch_gemini_test.py if you need the original prototype.")
sys.exit(0)
