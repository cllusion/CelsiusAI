"""Archived service account helper (disabled).

This file previously depended on google-auth. It's been disabled to avoid
requiring optional third-party packages during normal development. If you
need the original, restore from version control history.
"""

print("Archived service_account_auth (disabled)")
