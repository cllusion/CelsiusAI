# Archived service account helper (disabled)
# This file depends on google-auth; it's been archived/disabled to keep the
# main tree free from hard external dependencies. To re-enable, rename to
# service_account_auth.py and install google-auth.

# Original content available in repository history.
